/* this doesn't work, probably because it's a vcc compiler type of thing
 * instead just use this in one file, probably don't even need the static definitions
 */

// #define STB_IMAGE_IMPLEMENTATION
// #define STB_IMAGE_STATIC
// #include "stb_image.h"
